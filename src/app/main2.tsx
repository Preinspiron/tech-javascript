import React from 'react';
import ReactDOM from 'react-dom/client';
import AppCasino from './AppCasino.tsx';
import './casino.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AppCasino />
  </React.StrictMode>,
);
